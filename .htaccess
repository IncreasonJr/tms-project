# Prevent directory listing
Options -Indexes

# Deny direct access to config and secret files
<Files "config.php">
    Order Deny,Allow
    Deny from all
</Files>

<FilesMatch "\.(sql|log|sh|htaccess|htpasswd|\.env)$">
    Order Deny,Allow
    Deny from all
</FilesMatch>
